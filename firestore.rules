rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read,write: if
        auth.token.email == "donald.patterson@gmail.com" &&
        auth.token.email_verified == true;
    }
  }
}